import pandas as pd
pd.set_option('display.max_columns', None)
data = pd.read_csv('titanic.csv')
sfm = data[(data['Sex'] == 'female') & (data['Survived'] == 1) & (data['Pclass'] == 1)]
print(sfm.PassengerId.count())
m = data[(data['Age']<18) & (data['Parch'] == 0)]
print(m.PassengerId.count())
